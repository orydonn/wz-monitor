{
  "name": "wz-monitor",
  "version": "0.1.0",
  "description": "Workzilla freelancer task monitor → Telegram notifications",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --env-file=.env.local src/index.js",
    "capture": "node scripts/capture-session.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  }
}
